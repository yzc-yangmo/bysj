{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "cwd": "${workspaceFolder}/src",  // 关键修改：设置工作目录为 src
            "console": "integratedTerminal"
        }
    ]
}